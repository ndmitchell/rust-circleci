version: 2.1
orbs:
  rust: circleci/rust@1.6.0
jobs:
  build:
    docker:
      - image: cimg/rust:1.49.0
    steps:
      - run: sudo apt-get update && apt-get install libssl-dev
      - checkout
      - rust/clippy
      - rust/build
      - rust/test
